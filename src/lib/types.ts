export interface Work {
  slug: number;
  title: string;
  description: string;
  size: string;
}

export interface Series {
  slug: number;
  title: string;
  description: string;
}

export interface Sale {
  slug: number;
  title: string;
  description: string;
  size: string;
}
