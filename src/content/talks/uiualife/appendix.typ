#let appendix = [
  = Saving

  #figure(
    grid(
      inset: 0.5em,
      ```
      ≡Draw ∧(.Step) ⇡ T Init R    # run sim
      &fwa "out.gif" gif 48 - : 1  # save gif
      ```,
    ),
    caption: [How Sortsol was run (top) and saved to gif (bottom)],
  )
]
