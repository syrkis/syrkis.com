{
  "scripts": {
    "now-build": "next build && next export -o dist",
    "now-dev": "next -p $PORT"
  },
  "dependencies": {
    "next": "latest",
    "react": "latest",
    "react-dom": "latest",
    "react-helmet": "^6.1.0",
    "react-particles-js": "^3.4.1",
    "react-tilt": "latest"
  }
}
